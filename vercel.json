{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "web-build" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dist": "/index.html" }
  ]
}
